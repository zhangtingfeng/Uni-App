import { ClassMetaData, ClassMetadataBuilder } from 'shared-framework';
import { DashboardModel } from '../dashboard.model';

export class DashboardMetaData extends ClassMetadataBuilder<DashboardModel> {
    public ApplyImpl(target: ClassMetaData<DashboardModel>) {
        
    }
}
